*gemi.txt*  Neovim plugin for Google Gemini CLI integration

Author: Your Name
License: MIT

==============================================================================
CONTENTS                                                        *gemi-contents*

    1. Introduction .................. |gemi-introduction|
    2. Installation .................. |gemi-installation|
    3. Configuration ................. |gemi-configuration|
    4. Commands ...................... |gemi-commands|
    5. Functions ..................... |gemi-functions|
    6. Keybindings ................... |gemi-keybindings|
    7. Troubleshooting ............... |gemi-troubleshooting|

==============================================================================
1. INTRODUCTION                                             *gemi-introduction*

Gemi.nvim provides seamless integration with Google's gemini-cli, allowing
you to interact with Gemini AI directly from Neovim. The plugin features a
minimal, toggleable UI and comprehensive file change tracking.

Features:
- Easy gemini-cli installation and dependency management
- Minimal, non-intrusive UI
- Real-time execution with progress feedback
- File change tracking and navigation
- Side-by-side diff viewer
- Built-in Google authentication
- Customizable keybindings

==============================================================================
2. INSTALLATION                                             *gemi-installation*

Using lazy.nvim: >
    {
      'your-username/gemi.nvim',
      config = function()
        require('gemi').setup()
      end
    }
<

Using packer.nvim: >
    use {
      'your-username/gemi.nvim',
      config = function()
        require('gemi').setup()
      end
    }
<

After installation, run |:GemiInstall| to install dependencies.

==============================================================================
3. CONFIGURATION                                           *gemi-configuration*

Configure Gemi.nvim by calling setup() with your options: >

    require('gemi').setup({
      ui = {
        width = 0.8,
        height = 1,
        position = 'bottom',
        border = 'rounded',
        title = ' Gemi ',
        prompt = 'Gemi: ',
      },
      keymaps = {
        toggle = '<leader>g',
        files = '<leader>gf',
        diff = '<leader>gd',
        stop = '<leader>gs',
      },
      gemini = {
        model = 'gemini-pro',
        max_tokens = 4096,
        temperature = 0.7,
      },
      tracking = {
        auto_scan = true,
        exclude_patterns = {
          '%.git/',
          'node_modules/',
          '%.DS_Store',
          '%.log$',
        },
      },
    })
<

                                                               *gemi-config-ui*
ui.width            Width of the prompt window (0.0 to 1.0)
ui.height           Height of the prompt window (lines)
ui.position         Position: 'bottom', 'top', or 'center'
ui.border           Border style: 'single', 'double', 'rounded', etc.
ui.title            Window title text
ui.prompt           Prompt text shown in input

                                                         *gemi-config-keymaps*
keymaps.toggle      Keymap to toggle the UI (default: '<leader>g')
keymaps.files       Keymap to show changed files (default: '<leader>gf')
keymaps.diff        Keymap to show diff view (default: '<leader>gd')
keymaps.stop        Keymap to stop execution (default: '<leader>gs')

                                                           *gemi-config-gemini*
gemini.model        Gemini model to use (default: 'gemini-pro')
gemini.max_tokens   Maximum tokens for responses (default: 4096)
gemini.temperature  Response creativity (0.0 to 1.0, default: 0.7)

                                                         *gemi-config-tracking*
tracking.auto_scan      Auto-track file changes (default: true)
tracking.exclude_patterns   File patterns to exclude from tracking

==============================================================================
4. COMMANDS                                                     *gemi-commands*

                                                               *:GemiToggle*
:GemiToggle            Toggle the Gemi prompt interface

                                                               *:GemiInstall*
:GemiInstall           Install gemini-cli and check dependencies

                                                               *:GemiAuth*
:GemiAuth              Authenticate with Google

                                                               *:GemiFiles*
:GemiFiles             Show files changed by Gemi in quickfix list

                                                               *:GemiDiff*
:GemiDiff              Show side-by-side diff view of changes

==============================================================================
5. FUNCTIONS                                                   *gemi-functions*

                                                             *gemi.setup()*
gemi.setup({opts})     Configure the plugin with given options

                                                             *gemi.toggle()*
gemi.toggle()          Toggle the Gemi prompt interface

                                                         *gemi.install_cli()*
gemi.install_cli()     Install gemini-cli and dependencies

                                                         *gemi.authenticate()*
gemi.authenticate()    Start Google authentication flow

                                                   *gemi.show_changed_files()*
gemi.show_changed_files()  Show changed files in quickfix list

                                                           *gemi.show_diff()*
gemi.show_diff()       Show diff view of changes

                                                       *gemi.execute_prompt()*
gemi.execute_prompt(prompt)  Execute a Gemini prompt

                                                              *gemi.stop()*
gemi.stop()            Stop current Gemini execution

==============================================================================
6. KEYBINDINGS                                               *gemi-keybindings*

Default keybindings (can be customized in configuration):

{mode}  {lhs}           {rhs}                   {description}
n       <leader>g       |gemi.toggle()|        Toggle Gemi prompt
n       <leader>gf      |gemi.show_changed_files()| Show changed files
n       <leader>gd      |gemi.show_diff()|     Show diff view
n       <leader>gs      |gemi.stop()|          Stop execution
n       <leader>ga      |gemi.authenticate()|  Authenticate
n       <leader>gi      |gemi.install_cli()|   Install CLI

UI-specific keybindings (when prompt is open):
i       <Esc>           Close prompt
n       <Esc>           Close prompt
n       q               Close prompt
i       <C-c>           Close prompt

==============================================================================
7. TROUBLESHOOTING                                       *gemi-troubleshooting*

Problem: "gemini-cli not found"
Solution: Run |:GemiInstall| to install dependencies

Problem: "Node.js is not installed"
Solution: Install Node.js from https://nodejs.org/

Problem: Authentication fails
Solution:
- Run |:GemiAuth| to re-authenticate
- Check internet connection
- Ensure you have a Google account with Gemini access

Problem: No file changes detected
Solution:
- Execute a Gemi command after plugin installation
- Check exclude patterns in configuration
- Verify you're in a project directory

Problem: UI doesn't appear
Solution:
- Check Neovim version (0.8+ required)
- Verify plugin installation
- Check for conflicting keybindings

For more help, visit: https://github.com/your-username/gemi.nvim/issues

==============================================================================
vim:tw=78:ts=8:ft=help:norl:
